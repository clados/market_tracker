name: frontend
type: Load Balanced Web Service

# Application configuration
image:
  build: 
    dockerfile: ./frontend/Dockerfile
    context: .
  port: 5173

# Environment variables
variables:
  NODE_ENV: production
  VITE_USE_MOCK_DATA: false
  VITE_API_BASE_URL: ${BACKEND_URL}

# Health check
http:
  path: '/'
  port: 5173
  healthy_threshold: 2
  unhealthy_threshold: 2
  timeout: 5s
  interval: 30s

# Scaling configuration
cpu: 256
memory: 512
count: 1

# Network configuration
network:
  vpc:
    placement: public

# Route 53 integration (optional for staging)
# http:
#   alias: ${DOMAIN_NAME}
#   hosted_zone: ${HOSTED_ZONE_ID} 